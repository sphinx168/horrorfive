# 《五樓》流程總覽（快速查詢用）

> 這份文件是給 agent／開發者快速查目前遊戲流程用的**摘要**，不是設計來源。
> 完整的敘事理由、台詞、未拍板討論請查
> [`恐怖留言板遊戲_idea.md`](恐怖留言板遊戲_idea.md)（尤其第四、六、七章）；
> 實作細節請查 `五樓_單人版.html`。
>
> **維護規則：任何調動 `prog` 推進點、`GATES`、`AFTER`、`endReady()`、
> `CHAT` 場景觸發條件或 `picks` 分支的程式改動，都必須同步更新這份文件對應段落，
> 在同一個 commit／PR 裡一起改。** 如果不確定文件跟程式碼是否一致，可用
> `design-doc-sync` agent 抽查（它比對的是 idea.md，但邏輯上這份文件的
> 條列項目全部可回溯到 idea.md 第六、七章，出現落差同樣代表這份文件過期了）。

## prog 階段對照表

| prog | 章節 | 觸發方式 | 對應閘 |
|---|---|---|---|
| 0 | 序章 | 遊戲開始 | `g_mail` |
| 1 | 第一章前半 | `g_mail` 過關 | `g_ytl`（`g_admin` 前半，板務後台登入） |
| 2 | 第一章後半 | `g_admin` 過關（還原完成） | `g_search` |
| 3 | 第二章 | `g_search` 過關（搜尋到部落格） | 無（讀部落格＋證據） |
| 4 | 第二章末 | 目前程式碼未賦值（`curGate()` 仍接受此值） | — |
| 5 | 終章前 | 點開搜尋結果，`prog=5` 並開啟部落格 | 無 |
| 6 | 結局 | 送出終幕草稿泡泡，`prog=6` → `renderEnd` | 無 |

## 三道謎題（`GATES`）

1. **`g_mail`**（Yahoo 信箱登入）
   - 帳號鎖死 `siyu_1120`
   - 密碼提示「我的生日」→ 直接輸入 `1120` 會被拒絕
   - 正解：生日**倒過來打**，`0211`（線索：〈新來的那個〉19F ＋聊天室冠廷提示）
   - `needChat`: `c_intro`
   - 過關 → `AFTER.g_mail.unlockAt = 1`

2. **`g_admin`**（同一分頁兩段，`skin` 分別是 `admin`／`restore`）
   - 前半 `ADMIN_LOGIN`：帳號欄鎖死 `ytl_lin`，密碼藏在 2015/04〈五樓開好了〉信裡（`m_box`）
     - 過關旁白：「你現在是他。」
   - 後半：`adminIn=true` 後切換成還原清單（`restorables()`），玩家點選一列＋按確認還原
     - 正確選項：刪除者欄為「板主」的那筆（`r_sort`，對到 `t_sort` 20F）
     - 另外兩筆（`r_kt`／欣怡生日那句）真的可還原，非誘餌，不推進進度
   - `needChat`: `c_dig`
   - 過關 → `AFTER.g_admin.unlockAt = 2`，旁白只描述現象（不代下結論），並自動 `toJump()` 捲到 `jumpRid`

3. **`g_search`**（搜尋他的名字）
   - `needChat`: `c_who`
   - 找到部落格 → `AFTER.g_search.unlockAt = 3`，`prog=5` 時另外由點擊搜尋結果直接設定

> 真正的「謎題」不在密碼欄，而是 **`c_im` 選擇泡泡**：比對 10/14 即時通語氣忽然變短、
> 一句沒頭沒尾的「對不起」，玩家自己要選出「那天打字的不是她」——三道 GATES
> 都是**確認**，不是**發現**。

## 線性揭露鏈（§四）

1. 〈[整理] 她〉2015/10/14 求救樓下一樓被刪 → 缺口（`t_sort`，`at:0`）
2. 比對即時通存檔語氣 → 玩家在 `c_im` 選擇泡泡自己提出猜想
3. 登入舊信箱（`g_mail`）→ 確認自己是阿哲，拿到後台密碼
4. 用他的帳號進板務後台還原那一樓（`g_admin`）→ 20F 顯示 `ytl_lin`
5. `t_stop`／`t_after`＋映柔的信（`yr`）→ 傷害有多重
6. 信箱垃圾桶裡林昱庭寄的求助信（`ytl`，`at:1`，混在廣告信中間）→ 他求救過，她回了「沒有」
7. 搜尋他的名字（`g_search`）→ 部落格＋冠廷拍的三張照片（`d_box`，`at:5`）

## 茶水間（`CHAT`）觸發節點（重點場景）

| 場景 id | 觸發條件 `when()` | 作用 |
|---|---|---|
| `c_open` | 遊戲開始 | 日期引信，問板還在不在，拿到網址 |
| `c_pw` | `pwFail` | 密碼倒著打的提示（僅未拍板的玩家會觸發） |
| `c_intro` | 登入完成 | 箱子話題、文豪拒答退出，貼出〈整理〉連結 |
| `c_gap` | `seen.has('t_sort')` | 三人對刪除樓層的反應，思妤自己問出缺口 |
| `c_im` | 讀過十月存檔 | 語氣比對，玩家選擇泡泡下猜想 |
| `c_dig` | `prog>=1` | 冠廷自白 `r_kt`，帶出「試試看登入後台」動機 |
| `c_admin` | 進入後台那一刻 | 「是我在操作」／「紀錄上不會這樣寫」的規則 |
| `c_who` | `g_admin` 過關 | 首次講出 11/27，帶出「查他現在在哪」動機 |
| `c_letter` | 讀過 `ytl` 信 | 選擇泡泡：「沒有」／「他有寄過信給我」 |
| `c_react` | 讀過 `d_box` | 三人對調查改口反應 |
| `c_found` | `g_search` 過關 | 找到部落格，宗翰吐露自己心虛 |
| `c_push` | `endReady()` 未滿足但已達終章前置 | 補貼漏看的證據連結 |
| `c_end` | `endReady()` 且 `blogRead` | 終幕選擇泡泡，`prog=6` |

## 結局前置條件 `endReady()`

必須都點開過：
- `a_im`（十月即時通存檔）
- `t_stop`（〈收手〉討論串）
- `d_box`（冠廷拍的三張照片）
- `ytl`、`yr`（兩封關鍵信件）

外加 `blogRead=true`（部落格真的捲到底）。缺任一項不會卡死，`c_push` 會補貼一次連結。

## 玩家的三個選擇（`picks`）

| 選擇點 | 選項 | 影響 |
|---|---|---|
| `c_im`（10/14 是誰打字的） | 三選一猜想 | 反應不同，收回同一條線 |
| `c_letter`（讀完求助信後） | 「沒有」／「他有寄過信給我」 | 群組追問，宗翰坦承沒問過他 |
| `c_end`（終幕） | 指控／承認沒回信／繼續沉默 | 決定 `renderEnd` 開場段落（`picks.final`） |

## 相關 agent／skill

- 改流程前後想比對文件與實作是否同步 → `design-doc-sync` agent
- 改日期、樓層編號、揭露順序 → `narrative-continuity` agent
- 改任何對白 → `voice-check` agent
- 新增或改寫劇情文本 → `add-story` skill
- 校對介面文字／文件內文 → `text-review` skill
